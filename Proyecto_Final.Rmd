---
title: "Análisis de Regresión: Gasto Calórico"
author: "Nicolás de Silva Nacenta, Israel Rodríguez Donato, Andrés Manuel Roldán Navarro"
date: "29 de mayo de 2026"
output: pdf_document
---

```{r setup, include = F}

knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE
)

# Librerías para graficar
library(ggplot2)
library(ggthemes)
library(GGally)

# Librerías para realizar pruebas de hipótesis
library(nortest) #Pruebas de Normalidad
library(car) #NCV test
library(lmtest) #Durbin-Watson y Breusch-Godfrey
library(MASS) #Transformación BoxCox

library(patchwork)
library(data.table)
library(knitr)
library(corrplot)
library(dplyr)
library(purrr)
library(tidyr)

moda <- function(x) {
  x <- x[!is.na(x)]
  ux <- unique(x)
  ux[which.max(tabulate(match(x, ux)))]
}

```

# 1. Introducción

## 1.1. Contexto

La regresión lineal es un modelo que permite construir una relación lineal entre una variable "dependiente" y un conjunto de variables "independientes". Esta relación permite explicar el comportamiento de la variable "dependiente" y predecir valores nuevos que podría tomar.

## 1.2. Objetivo general

Este proyecto tiene por objetivo ajustar modelos de regresión lineal que expliquen el número de calorías quemadas en función de variables de interés, así como evaluar el ajuste de estos modelos, que tan bien cumplen los supuestos de la regresión y la viabilidad de los modelos que se propongan.

## 1.3. Objetivos específicos

-   Ajustar un modelo de regresión lineal simple.
-   Ajustar un modelo de regresión lineal múltiple.
-   Comparar ambos modelos.

# 2. Descripción de los datos

## 2.1. Variables

- Calories: Calorias
- Gender: Género
- Age: Edad 
- Height: Altura (cm)
- Weight: Peso (Kg)
- BMI: Índice de masa corporal ($\frac{Kg}{m^2}$)
- Duration: Duración de actividad física (min)
- Heart_Rate: Frecuencia cardíaca (lpm)
- Body_Temp: Temperatura corporal (C°)

## 2.2. Carga de datos y visualización de la base de datos

```{r, echo=FALSE}
calories <- read.csv("https://raw.githubusercontent.com/ndsn-alv/Modelos_Regresion/main/Data/calorias.csv")

kable(
  head(calories, n = 10),
  caption = "10 observaciones de la base",
  digits = 2
)

```

# 3. Análisis descriptivo de datos

## 3.1. Estadísticos descriptivos

```{r}

cal_desc <- data.frame(
  Media = sapply(calories[,-2], mean, na.rm = TRUE),
  Mediana = sapply(calories[,-2], median, na.rm = TRUE),
  Moda = sapply(calories[,-2], moda),
  Varianza = sapply(calories[,-2], var, na.rm = TRUE),
  Desviacion_Estandar = sapply(calories[,-2], sd, na.rm = TRUE)
)

kable(
  cal_desc,
  caption = "Estadísticos descriptivos",
  digits = 2
)

```

## 3.2. Visualización de variables

```{r}
par(mfrow = c(3,3))

for (v in colnames(calories[,-2])) {
  hist(calories[[v]],
       main = paste("Histograma de", v),
       xlab = v,
       col = "lightgray",
       border = "black")
}

barplot(table(calories$Gender),
        main = "Frecuencia de Gender",
        xlab = "Gender",
        ylab = "Frecuencia",
        col = "lightgray",
        border = "black"
)

```

Matriz de correlación:

```{r}

cal_male <- calories %>% filter(Gender == "male")
cal_male <- cal_male[,-2]

cal_female <- calories %>% filter(Gender == "female")
cal_female <- cal_female[,-2]

par(mfrow = c(1,2),
    mar = c(1,1,3,1))

corrplot(cor(cal_male),
         method = "number",
         type = "upper",
         tl.col = "black",
         tl.cex = 0.7,  
         number.cex = 0.6,
         title = "Hombres",
         mar = c(0,0,2,0))

corrplot(cor(cal_female),
         method = "number",
         type = "upper",
         tl.col = "black",
         tl.cex = 0.7,
         number.cex = 0.6,
         title = "Mujeres",
         mar = c(0,0,2,0))
         
```
Se pueden observar tres grupos de variables correlacionadas entre si para ambos género. Consisten en:

- Grupo 1: *Calories, Duration, Heart_Rate, Body_Temp*
- Grupo 2: *Height, Weight, BMI*
- Grupo 3: *Age*

Se puede ver que la edad no esta correlacionada con ninguna de las otras variables. Podria no ser significante para la quema de calorias. La altura, peso e índice de masa corporal son caracateristicas físicas de la persona en cuestión, además el índice de masa corporal es directamente calculada usando las otras dos variables. Por último, la duración, frecuencia cardíaca y temperatura corporal son consecuencia del ejercicio en cuestión lo cual nos lleva a pensar que son los que mas afectan a la quema de calorias. 

Analizando las matrices notamos que entre ambos géneros no existe gran diferencia en la correlación lo cual podría explicar que ningun género presenta ventaja sobre el otro a la hora de la quema de calorias.

# 4. Modelo de Regresión Lineal Simple

El modelo de regresión lineal simple construye la relación entre la variable dependendiente y una sola variable independiente.

## 4.1. Selección de variable explicativa

Al realizar la rutina de ejercicio, el gasto calorico podría ser explicado por alguna de las siguientes variables numéricas, seleccionadas por tener una alta correlación con el gasto calórico:
* Duration: tiene sentido pensar que mientras más tiempo se haya hecho ejercicio, mayo será el gasto calórico.
* Heart_Rate: el número de latidos por minuto puede funcionar como una métrica del esfuerzo realizado por el individuo durante la rutina, esperaríamos que mayor esfuerzo, mayor gasto calorico.
* Body_Temp: al igual que el número de latidos, una alta temperatura corporal indicaría un mayor gasto calorico, debido al esfuerzo realizado.

<!--- 
Para sustentar nuestra intuición, graficaremos estos modelos propuestos:
--->

```{r, echo = FALSE}
p1 <- ggplot(calories, aes(x = Duration, y = Calories)) +
  geom_point(alpha = 0.6) +
  geom_smooth(method = "lm", 
              color = "red",
              se = FALSE) +
  geom_smooth(method = "lm", 
              formula = y ~ I(x^2), 
              color = "blue", 
              se = FALSE) +
  labs(title = "Gasto Calorico ~ Duracion",
       x = "Duracion",
       y = "Gasto Calorico") +
  theme(plot.title = element_text(hjust = 0.5)) +
  theme_minimal()

p2 <- ggplot(calories, aes(x = Heart_Rate, y = Calories)) +
  geom_point(alpha = 0.6) +
  geom_smooth(method = "lm", 
              color = "red",
              se = FALSE) +
  geom_smooth(method = "lm", 
              formula = y ~ I(x^2), 
              color = "blue", 
              se = FALSE) +
  labs(title =  "Gasto Calorico ~ Frecuencia Cardiaca",
       x = "Frecuencia Cardiaca",
       y = "Gasto Calorico") +
  theme(plot.title = element_text(hjust = 0.5)) +
  theme_minimal()

p3  <- ggplot(calories, aes(x = Body_Temp, y = Calories)) +
  geom_point(alpha = 0.6) + # alpha ayuda a ver la densidad de puntos empalmados
  geom_smooth(method = "lm", 
              color = "red",
              se = FALSE) +
  geom_smooth(method = "lm", 
              formula = y ~ I(x^2), 
              color = "blue", 
              se = FALSE) +
  labs(title = "Gasto Calorico ~ Temperatura Corporal",
       x = "Temperatura Corporal",
       y = "Gasto Calorico") +
  theme(plot.title = element_text(hjust = 0.5)) +
  theme_minimal()

p1
p2
p3

```

<!---
Los modelos más prometedores son el basado en duración y el basado en frecuencia cardíaca, ya sea usando x sin transformar o usando x^2.
--->

## 4.2. Evaluación y ajuste del modelo

<!---
Después de una inspección visual, plantearemos cuatro modelos:
calorias ~ duración
calorias ~ duración^2
calorias ~ frecuencia
calorias ~ fracuencia^2
--->


```{r echo=FALSE}

mod_d <- lm(formula = Calories ~ Duration, data = calories)
mod_d2 <- lm(formula = Calories ~ I(Duration^2), data = calories)
mod_hr <- lm(formula = Calories ~ Heart_Rate, data = calories)
mod_hr2 <- lm(formula = Calories ~ I(Heart_Rate^2), data = calories)

summary(mod_d)
summary(mod_d2)
summary(mod_hr)
summary(mod_hr2)

```
<!---
Los modelos obtuvieron resultados excelentes, todos los coeficientes estimados son estadísticamente significativos, el coeficiente de ajuste es mayor al 90% en los modelos basados en duración y mayor a 80% en los modelos basados en frecuencia cardíaca.

--->

## 4.4. Supuestos del modelo

```{r}
# La siguiente función entrega las pruebas de hipótesis necesarias para evaluar los supuestos de cualquier modelo
evaluar_supuestos <- function(modelo) {
  residuos <- residuals(modelo)
  
  shapiro_res <- shapiro.test(residuos)
  ncv_res <- car::ncvTest(modelo)
  dw_res <- lmtest::dwtest(modelo)
  bg_res <- lmtest::bgtest(modelo)
  
  resultados <- list(
    shapiro_wilk = shapiro_res,
    ncv_homocedasticidad = ncv_res,
    durbin_watson = dw_res,
    breusch_godfrey = bg_res
  )
  
  return(resultados)
}
```

```{r}

print("Modelos Basados en Duración")
evaluar_supuestos(mod_d)
print("^2")
evaluar_supuestos(mod_d2)

print("Modelos Basados en Frecuencia Cardíaca")
evaluar_supuestos(mod_hr)
print("^2")
evaluar_supuestos(mod_hr2)

par(mfrow = c(2,2))
plot(mod_d)
plot(mod_d2)
plot(mod_hr)
plot(mod_hr2)

AIC(mod_d,mod_d2,mod_hr,mod_hr2)
BIC(mod_d,mod_d2,mod_hr,mod_hr2)

```

<!---En general, podemos notar que ningún modelo sale bien librado, los principales problemas están relacionados con homocedasticidad y normalidad de los errores. Tiene sentido, pues si observamos nuestros gráficos, podemos notar que los datos, a pesar de tener un coeficiente de ajuste alto, no se adecúan a la forma de una línea recta.

Para solucionar este problema, podemos intentar una transformación BoxCox de los datos, buscando solucionar los problemas de homocedasticidad y normalidad, aunque, gráficamente, un modelo más adecuado sería un polinomio, particularmente uno de grado 2.
--->

```{r echo=FALSE}

bc_d <- boxcox(mod_d, plotit = FALSE)
bc_hr <- boxcox(mod_hr, plotit = FALSE)

lambda_d   <- bc_d$x[which.max(bc_d$y)]
lambda_hr <- bc_hr$x[which.max(bc_hr$y)]

<<<<<<< HEAD
cat("lambda (Duration)   :", lambda_d,   "\n")
cat("lambda (Heart_Rate) :", lambda_hr, "\n")
=======
<<<<<<< HEAD
cat("lambda (Duration)   :", lambda_duration,   "\n")
cat("lambda (Heart_Rate) :", lambda_heart_rate, "\n")
>>>>>>> cef2eb84928f04fd70b181a15ba4734f8cd36ba8

# Función auxiliar para aplicar la transformación Box-Cox sobre Y
transf_boxcox <- function(y, lambda) {
  if (abs(lambda) < 1e-6) log(y) else (y^lambda - 1) / lambda
}

# Transformamos Calories con el lambda de cada modelo candidato

calories$Cal_trans_dur  <- transf_boxcox(calories$Calories, lambda_d)
calories$Cal_trans_hr   <- transf_boxcox(calories$Calories, lambda_hr)

# Re-ajuste de los tres modelos con Y transformada
mod_duration_bc   <- lm(Cal_trans_dur ~ Duration,   data = calories)
mod_heart_rate_bc <- lm(Cal_trans_hr  ~ Heart_Rate, data = calories)

summary(mod_duration_bc)
summary(mod_heart_rate_bc)
<<<<<<< HEAD

cat("lambda (Duration)   :", lambda_d,   "\n")
cat("lambda (Heart_Rate) :", lambda_hr, "\n")
=======
=======
cat("lambda (Duration)   :", lambda_d,   "\n")
cat("lambda (Heart_Rate) :", lambda_hr, "\n")
>>>>>>> da91fea8fd4c2fa655dfc9914636fb3ca1789ffe
>>>>>>> cef2eb84928f04fd70b181a15ba4734f8cd36ba8

```

<!---

Dado que obtuvimos una lambda de 0.5, aplicaremos la siguiente transformación a las calorias: sqrt(Calories)

--->

```{r echo = FALSE}

calories$Calories_T <- sqrt(calories$Calories)

mod_d_bc  <- lm(Calories_T ~ Duration, calories)
mod_hr_bc <- lm(Calories_T ~ Heart_Rate, calories)

ggplot(data = calories, aes(x = Duration, y = Calories_T))+
  geom_point(alpha = 0.6)+
  geom_smooth(method = "lm",
              se = FALSE,
              color = "red")+
  labs(title = "√Calorias ~ Duración")+
  theme_minimal()

ggplot(data = calories, aes(x = Heart_Rate, y = Calories_T))+
  geom_point(alpha = 0.6)+
  geom_smooth(method = "lm",
              se = FALSE,
              color = "red")+
  labs(title = "√Calorias ~ Frecuencia Cardiaca")+
  theme_minimal()

par(mfrow = c(2,2))
plot(mod_d_bc)
plot(mod_hr_bc)


print("Modelo Basado en Duración")
summary(mod_d_bc)
print("Modelo Basado en Frecuencia Cardiaca")
summary(mod_hr_bc)

AIC(mod_d_bc, mod_hr_bc)
BIC(mod_d_bc, mod_hr_bc)

print("Modelo Basado en Duración")
evaluar_supuestos(mod_d_bc)
print("Modelo Basado en Frecuencia Cardiaca")
evaluar_supuestos(mod_hr_bc)

```


## 4.5. Intervalos de confianza (95 %)

## 4.6. Intervalos para respuesta media y predicción

# 5. Modelo de Regresión Lineal Múltiple

## 5.1. Selección de variables

Para una selección inicial definimos un modelo utilizando todas las variables. Mediante un stepwise AIC con dirección hacia atras verificamos que eliminando la varible *Gender* obtenemos un menor AIC que el original. Posiblemente debido a que realmente el género no afecta fuertemente en la correlación del resto de variables como se vio en la matriz de correalación. 

## 5.2. Ajuste del modelo

```{r}
mod <- lm(Calories ~ Age + Height + Weight + BMI + Duration + Heart_Rate + Body_Temp, calories)
mod_summary <- summary(mod)


coeffs <- data.frame(
  mod_summary$coefficients
)

kable(
  coeffs,
  caption = "Resumen del modelo",
  digits = 6
)

```
Adicionalmente obtenemos estas caracteristicas:

- Multiple R-squared: 0.9723
- Adjusted R-squared: 0.9713
- p-valor conjunto: 0

Esto nos indica que obtenemos un modelo múltiple estadísticamente significativo.

## 5.3. Evaluación de multicolinealidad del modelo

Desde la matriz de correlación obteniamos distinto grupos de variables, lo cual es posible que se presnete alta multicolinealidad. Obtenemos los siguientes valores de inflación de la varianza para cada variable.

```{r}

mod_vif <- data.frame(
  VIF = vif(mod)
)

kable(
  mod_vif,
  caption = "Valores de Inflación de la Varianza (VIF)"
)
```

Podemos ver que el modelo tiene multicolinealidad fuerte en las variables *Height*, *Weight* y *BMI*, donde directamente el índice de masa corporal es una relación entre la altura y el peso lo cual explica la alta correlación entre este grupo de variables. Por otro lado, variables como *Duration*, *Heart_Rate* y *Body_Temp* presentan multicolinealidad moderada. Igualmente por la matriz de correlación observamos una correlación fuerte entre este otro grupo de variables. Por último, la variable *Age* mantiene el VIF mas pequeño, consistente con su nula o débil correlación con el resto de las variables.

Buscamos ajustar un nuevo modelo mediante la eliminación de algunas de las variables de grupos correlacionados. Primero nos enfocamos en eliminar la multicolinealidad mas fuerte con las variables de masa corporal. Como resultado obte

```{r}
mod1 <- lm(Calories ~ Age + Weight + BMI + Duration + Heart_Rate + Body_Temp, calories)
mod2 <- lm(Calories ~ Age + Height + BMI + Duration + Heart_Rate + Body_Temp, calories)
mod3 <- lm(Calories ~ Age + Height + Weight + Duration + Heart_Rate + Body_Temp, calories)
mod4 <- lm(Calories ~ Age + Height + Duration + Heart_Rate + Body_Temp, calories)
mod5 <- lm(Calories ~ Age + Weight + Duration + Heart_Rate + Body_Temp, calories)
mod6 <- lm(Calories ~ Age + BMI + Duration + Heart_Rate + Body_Temp, calories)
mod7 <- lm(Calories ~ Age + Duration + Heart_Rate + Body_Temp, calories)

modelos <- list(
  mod1 = mod1,
  mod2 = mod2,
  mod3 = mod3,
  mod4 = mod4,
  mod5 = mod5,
  mod6 = mod6,
  mod7 = mod7
)

vif_long <- imap_dfr(modelos, ~{
  v <- vif(.x)
  tibble(
    Modelo = .y,
    Variable = names(v),
    VIF = as.numeric(v)
  )
})

orden_vars <- c("Age", "Height", "Weight", "BMI", 
                "Duration", "Heart_Rate", "Body_Temp")

vif_wide <- vif_long %>%
  mutate(Variable = factor(Variable, levels = orden_vars)) %>%
  arrange(Variable) %>%
  pivot_wider(names_from = Modelo, values_from = VIF)

kable(
  vif_wide,
  caption = "Nuevos VIFs"
)

```

Lo principal que podemos notar es que la multicolinealidad de los otros grupos de variables no fue afectada al remover estas variables. Además, quitando solo una nos basta para corregir multicolinealidad (excepto para el modelo 3). Mostramos ahora los AICs para cada modelo propuesto.

```{r}

aic1 <- AIC(mod1, mod2, mod3, mod4, mod5, mod6, mod7)

kable(
  aic1,
  caption = "AICs de los modelos"
)
```

No tenemos alteraciones muy fuertes en los AICs. Sin embargo, el modelo con el AIC mas bajo resulta ser aquello en donde eliminamos completamente el grupo de variables. Aún así, otra combinación de variables de masa corporal no debería afectar fuertemente al modelo.

```{r}

mod1 <- lm(Calories ~ Age + Heart_Rate + Body_Temp, calories)
mod2 <- lm(Calories ~ Age + Duration + Body_Temp, calories)
mod3 <- lm(Calories ~ Age + Duration + Heart_Rate, calories)
mod4 <- lm(Calories ~ Age + Body_Temp, calories)
mod5 <- lm(Calories ~ Age + Heart_Rate, calories)
mod6 <- lm(Calories ~ Age + Duration, calories)

modelos <- list(
  mod1 = mod1,
  mod2 = mod2,
  mod3 = mod3,
  mod4 = mod4,
  mod5 = mod5,
  mod6 = mod6
)

vif_long <- imap_dfr(modelos, ~{
  v <- vif(.x)
  tibble(
    Modelo = .y,
    Variable = names(v),
    VIF = as.numeric(v)
  )
})

orden_vars <- c("Age", "Duration", "Heart_Rate", "Body_Temp")

vif_wide <- vif_long %>%
  mutate(Variable = factor(Variable, levels = orden_vars)) %>%
  arrange(Variable) %>%
  pivot_wider(names_from = Modelo, values_from = VIF)

kable(
  vif_wide,
  caption = "Nuevos VIFs"
)


### NOTAS

# Un modelo en el que se elimina Duration y se deja Heart_Rate y Body_temp cumple normalidad
# Un modelo en el que se elimina Duration y Heart_Rate y se deja Body_temp cumple homocedasticidad
# Ambos cumplen autocorrelacion
# Boxcox no mejora los supuestos
# Quitando Age no se rechaza homcedasticidad

###

```


A excepción del modelo 2, basta quitar una sola variable para eliminar multicolinealidad. El modelo escogido será justificado en la siguiente sección ya que interesantemente los supuestos del modelo son afectados fuertemente dependiendo de la combinación de estas tres  especificamente.

## 5.4. Análisis de residuos

Haciendo prueba y error con cada uno de los modelos se encontró cuestiones muy interesantes sobre la relación entre *Duration*, *Heart_Rate* y *Body_Temp* y el cumplimiento de los supuestos.

Primero que nada, para cada una de las combinaciones se cumplía el supuesto de no autocorrelación al no rechazarse la prueba de Durbin-Watson mostrada mas adelante. 

Sin embargo, lo curioso empieza cuando un modelo que solo incluya *Body_Temp* como parte del grupo de variables era la única forma de mantener varianza constante (homocedasticidad). Pero anulaba la normalidad de los residuos teniendo p-valores muy cercanos a cero en las pruebas de normalidad que se mencionarán mas adelante.

Por último, la mejor combinación se encontró cuando eliminando *Duration* y dejando las otras dos variables. Esto no solo logro cumplir los supuestos de normalidad y no autocorrelación. Si no con unos p-valores cercanos a 1 en ambos casos. No importaba si se removian o añadian otras valores fuera de este grupo (*BMI* o *Gender*, por ejemplo). Mientras *Duration* no esté en el modelo, no podiamos negar normalidad ni autocorrelación. El problema era la homocedasticidad. Esta era brutalmente rechazada. Una vez que se notó que *Age*, en cierto punto afectaba la homocedasticidad de forma leva se optó por removerla completamente. Una vez que se cumplían todos los supuestos, se encontró una mejora todavía mayor al elevar al cuadrado tanto *Heart_Rate* como *Body_Temp* y una mejora todavía mayor incluyendo *BMI* (con *Height* o *Weight* se rechazaba homocedasticidad o tenian p-valores muy cercanos al nivel de significancia de 5%).

Ajustando el modelo se obtienen las siguientes caracteristicas.

```{r}

mod <- lm(Calories ~ BMI + I(Heart_Rate^2) + I(Body_Temp^2), calories)
mod_summary <- summary(mod);mod_summary

coeffs <- data.frame(
  mod_summary$coefficients
)

kable(
  coeffs,
  caption = "Resumen del nuevo modelo",
  digits = 6
)

```
- Multiple R-squared: 0.8941
- Adjusted R-squared: 0.8924
- P-valor conjunto: 0

**Normalidad.** Se evalúa si los residuales siguen una distribución aproximadamente normal. Mediante un QQ-plot, un histograma de frecuencias y un boxplot, para una interpretación visual. Se usan las pruebas de normalidad de Lilliefors, Cramér-von Mises, Anderson-Darling y Shapiro-Wilk.

```{r}
par(mfrow = c(1, 3))

qqnorm(residuals(mod))
qqline(residuals(mod))

hist(residuals(mod), breaks = 20)

boxplot(residuals(mod), main = "Boxplot")


norm_tests <- data.table(
  Test = c(
    "Lilliefors", 
    "Cramér-von Mises", 
    "Anderson-Darling", 
    "Shapiro-Wilk"
  ),
  p_value = c(
    lillie.test(mod$residuals)$p.value,
    cvm.test(mod$residuals)$p.value,
    ad.test(mod$residuals)$p.value,
    shapiro.test(mod$residuals)$p.value
  )
)

kable(
  norm_tests,
  caption = "Pruebas de normalidad",
  digits = 4
)
```
Visualmente, todo se ve excelente. En cuanto a las pruebas, absolutamente no hay forma de rechazar la hipótesis de normalidad. Los errores siguen una distribución normal.

**Homocedasticidad**. Se muestra el gráfico de dispersión de residuales contra valores ajustados, así como las pruebas formales de homocedasticidad.

```{r}
df <- data.frame(
  fitted = fitted(mod),
  resid = residuals(mod)
)

ggplot(df, aes(x = fitted, y = resid)) +
  geom_point()

hc_test <- data.frame(
  Test = c("Breusch-Pagan", "NCV"),
  p_values = c(bptest(mod)$p.value, ncvTest(mod)$p)
)

kable(
  hc_test,
  caption = "Pruebas de homocedasticidad",
  digits = 4
)

```

Visualmente, no podriamos decir que tengamos una dispersión unforme de los puntos. Mas bien, se ve una relación lineal en los residuales con los valores ajustados menores a cero, esto nos causa cierto grado de confusión especialmente por que no esperamos una cantidad negativa de calorias quemadas. Además, errores positivos se observan mas en los extremos de la distribución de las calorias, indicando una subestimación para valores muy grandes o pequeños en la quema de calorias. Por otro lado, en los valores centrales de la distribución se observa sobreestimación en las calorias quemadas. Esto podría deberse a que hay muchas observaciones cuya cantidad de calorias es muy cercana a cero. Esto podría ser problemático ya que es como si estuvieramos mediando la quema de calorias en gente que no hizo ejercicio o tuvo una rutina extremadamente corta (menor a 3 minutos). Aún asi, por las pruebas de homocedasticidad no podemos rechazar que se tenga varianza constante en el modelo.

**No autocorrelación**. Finalmente, se evalúa la presencia de autocorrelación en los residuales mediante la prueba correspondiente.

```{r}
autocor_test <- data.frame(
  Test = "Durbin-Watson",
  p_value = dwtest(mod)$p.value
)

kable(
  autocor_test,
  caption = "Prueba de no autocorrelación"
)

```

Podemos observar que no podemos rechazar la autocorrelación de los errores residuales obteniendo un p-valor muy alto. 

Este modelo nos permitió medir el gasto calórico por medio del índice de masa corporal, la frecuencia cardiaca y la temperatura corporal a la hora del ejercicio. 

## 5.5. Valores influyentes

```{r}

influencePlot(mod)
```

# 6. Comparación de modelos

# 7. Conclusiones
