---
title: "Análisis de Regresión: Gasto Calórico"
author: "Nicolás de Silva Nacenta, Israel Rodríguez Donato, Andrés Manuel Roldán Navarro"
date: "29 de mayo de 2026"
output: pdf_document
---
  
```{r setup, include = F}

knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE
)
library(car)
library(data.table)
library(ggplot2)
library(GGally)
library(knitr)
library(corrplot)
library(MASS)
library(dplyr)
library(nortest)
library(lmtest)

moda <- function(x) {
  x <- x[!is.na(x)]
  ux <- unique(x)
  ux[which.max(tabulate(match(x, ux)))]
}

```

# 1. Introducción

## 1.1. Objetivo general
Analizar el número de calorías quemadas en función de variables explicativas.

## 1.2. Objetivos específicos
- Ajustar un modelo de regresión lineal simple.
- Ajustar un modelo de regresión lineal múltiple.
- Comparar ambos modelos.

# 2. Descripción de los datos

## 2.1. Variables
- Calories: Calorias
- Gender: Género
- Age: Edad
_ Height: Altura (cm)
- Weight: Peso (Kg)
- BMI: Índice de masa corporal ($\frac{Kg}{m^2}$)
- Duration: Duración de actividad física (min)
- Heart_Rate: Frecuencia cardíaca (lpm)
- Body_Temp: Temperatura corporal (C°)

## 2.2. Carga de datos y visualización de la base de datos

```{r, echo=FALSE}
calories <- read.csv("https://raw.githubusercontent.com/ndsn-alv/Modelos_Regresion/main/Data/calorias.csv")

kable(
  head(calories, n = 10),
  caption = "10 observaciones de la base",
  digits = 2
)

```

# 3. Análisis descriptivo de datos

## 3.1. Estadístico descriptivos

```{r}

cal_desc <- data.frame(
  Media = sapply(calories[,-2], mean, na.rm = TRUE),
  Mediana = sapply(calories[,-2], median, na.rm = TRUE),
  Moda = sapply(calories[,-2], moda),
  Varianza = sapply(calories[,-2], var, na.rm = TRUE),
  Desviacion_Estandar = sapply(calories[,-2], sd, na.rm = TRUE)
)

kable(
  cal_desc,
  caption = "Estadísticos descriptivos",
  digits = 2
)

```

## 3.2. Visualización de variables

```{r}
par(mfrow = c(3,3))

for (v in colnames(calories[,-2])) {
  hist(calories[[v]],
       main = paste("Histograma de", v),
       xlab = v,
       col = "lightgray",
       border = "black")
}

barplot(table(calories$Gender),
        main = "Frecuencia de Gender",
        xlab = "Gender",
        ylab = "Frecuencia",
        col = "lightgray",
        border = "black"
)

```

Matriz de correlación:

```{r}

cal_male <- calories %>% filter(Gender == "male")
cal_male <- cal_male[,-2]

cal_female <- calories %>% filter(Gender == "female")
cal_female <- cal_female[,-2]

par(mfrow = c(1,2),
    mar = c(1,1,3,1))

corrplot(cor(cal_male),
         method = "number",
         type = "upper",
         tl.col = "black",
         tl.cex = 0.7,  
         number.cex = 0.6,
         title = "Hombres",
         mar = c(0,0,2,0))

corrplot(cor(cal_female),
         method = "number",
         type = "upper",
         tl.col = "black",
         tl.cex = 0.7,
         number.cex = 0.6,
         title = "Mujeres",
         mar = c(0,0,2,0))
         
```
Se pueden observar tres grupos de variables correlacionadas entre si para ambos género. Consisten en:

- Grupo 1: *Calories, Duration, Heart_Rate, Body_Temp*
- Grupo 2: *Height, Weight, BMI*
- Grupo 3: *Age*

Se puede ver que la edad no esta correlacionada con ninguna de las otras variables. Podria no ser significante para la quema de calorias. La altura, peso e índice de masa corporal son caracateristicas físicas de la persona en cuestión, además el índice de masa corporal es directamente calculada usando las otras dos variables. Por último, la duración, frecuencia cardíaca y temperatura corporal son consecuencia del ejercicio en cuestión lo cual nos lleva a pensar que son los que mas afectan a la quema de calorias. 

Analizando las matrices notamos que entre ambos géneros no existe gran diferencia en la correlación lo cual podría explicar que ningun género presenta ventaja sobre el otro a la hora de la quema de calorias.

# 4. Modelo de Regresión Lineal Simple

## 4.1. Selección de variable explicativa

## 4.2. Ajuste del modelo

## 4.3. Evaluación del modelo

## 4.4. Supuestos del modelo

## 4.5. Intervalos de confianza (95 %)

## 4.6. Intervalos para respuesta media y predicción

# 5. Modelo de Regresión Lineal Múltiple

## 5.1. Selección de variables

Para una selección inicial definimos un modelo utilizando todas las variables y mediante un proceso de stepwise AIC hacia atras, obtenemos un AIC inicial de 965.38. Luego, definiendo un modelo nulo, mediante stepwise AIC hacia adelante, obtenemos un AIC inicial de 1666.86.

En nuestro caso el modelo con el AIC mas bajo resulta ser ajustando el modelo explicando las calorias utilizando todas las variables numéricas y eliminando el género. Obteniendo un valor AIC de 963.42. Esto podria ser ya que como vimos en las matrices de correlación que el género no inflye fuertemente en la quema de calorias. 

## 5.2. Ajuste del modelo

```{r}
mod <- lm(Calories ~ Age + Height + Weight + BMI + Duration + Heart_Rate + Body_Temp, calories)
mod_summary <- summary(mod)


coeffs <- data.frame(
  mod_summary$coefficients
)

kable(
  coeffs,
  caption = "Resumen del modelo",
  digits = 6
)

```
Adicionalmente obtenemos estas caracteristicas:

- Multiple R-squared: 0.9723
- Adjusted R-squared: 0.9713
- p-valor conjunto: $<2.2\times 10^{-16}$

## 5.3. Evaluación de multicolinealidad del modelo

```{r}

mod_vif <- data.frame(
  VIF = vif(mod)
)

kable(
  mod_vif,
  caption = "Valores de Inflación de la Varianza (VIF)"
)
```

Podemos ver que el modelo tiene multicolinealidad fuerte en las variables *Height*, *Weight* y *BMI*, donde directamente el índice de masa corporal es una relación entre la altura y el peso lo cual explica la alta correlación entre este grupo de variables. Por otro lado, variables como *Duration*, *Heart_Rate* y *Body_Temp* presentan multicolinealidad moderada. Igualmente por la matriz de correlación observamos una correlación fuerte entre este otro grupo de variables. Por último, la variable *Age* mantiene el VIF mas pequeño, consistente con su nula o débil correlación con el resto de las variables.

Buscamos ajustar un nuevo modelo mediante la eliminación de algunas de las variables de grupos correlacionados. Como primer intento elimaremos una variable del grupo de duración, frecuencia cardíaca y temperatura corporal. Como resultado obtenemos los nuevos VIFs.

```{r}
mod1 <- lm(Calories ~ Age + Height + Weight + BMI + Heart_Rate + Body_Temp, calories)
mod2 <- lm(Calories ~ Age + Height + Weight + BMI + Duration + Body_Temp, calories)
mod3 <- lm(Calories ~ Age + Height + Weight + BMI + Duration + Heart_Rate, calories)
mod4 <- lm(Calories ~ Age + Height + Weight + BMI  + Body_Temp, calories)
mod5 <- lm(Calories ~ Age + Height + Weight + BMI + Duration, calories)
mod6 <- lm(Calories ~ Age + Height + Weight + BMI + Heart_Rate, calories)
mod7 <- lm(Calories ~ Age + Height + Weight + BMI, calories)

mods_vifs <- data.frame(
  Variable = c("Age", "Height", "Weight", "BMI", "Duration", "Heart_Rate", "Body_Temp"),
  Modelo_1 = c(1.093, 134.024, 217.516, 27.726, "...", 2.397, 2.434),
  Modelo_2 = c(1.113, 133.558, 216.875, 27.603, 5.665, "...", 5.736),
  Modelo_3 = c(1.078, 133.349, 216.433, 27.530, 4.058, 4.045, "...")
)


kable(
  mods_vifs,
  caption = "Nuevos VIFs"
)

```
Obtenemos los siguientes AICs:

- Modelo 1: 1802.54
- Modelo 2: 1645.309
- Modelo 3: 1577.17

El modelo 3 es el que menor AIC tiene, sin embargo por cuestiones que se discutiran en la siguiente sección escogemos el modelo que excluye la variable *Duration* (Modelo 1). Ahora nos enfocamos en el otro grupo de variables relacionados con el índice de masa corporal.

```{r}

mod1 <- lm(Calories ~ Age + Weight + BMI + Heart_Rate + Body_Temp, calories)
mod2 <- lm(Calories ~ Age + Height + BMI + Heart_Rate + Body_Temp, calories)
mod3 <- lm(Calories ~ Age + Height + Weight + Heart_Rate + Body_Temp, calories)
mod4 <- lm(Calories ~ Age + Height + Heart_Rate + Body_Temp, calories)
mod5 <- lm(Calories ~ Age + Weight + Heart_Rate + Body_Temp, calories)
mod6 <- lm(Calories ~ Age + BMI + Heart_Rate + Body_Temp, calories)
mod7 <- lm(Calories ~ Age + Heart_Rate + Body_Temp, calories)

### NOTAS

# Un modelo en el que se elimina Duration y se deja Heart_Rate y Body_temp cumple normalidad
# Un modelo en el que se elimina Duration y Heart_Rate y se deja Body_temp cumple homocedasticidad
# Ambos cumplen autocorrelacion
# Boxcox no mejora los supuestos (debe ser alguna cuestion polinomial)

###

mods_vifs <- data.frame(
  Variable = c("Age", "Height", "Weight", "BMI", "Heart_Rate", "Body_Temp"),
  Modelo_1 = c(1.092, "...", 2.156, 2.266, 2.387, 2.387),
  Modelo_2 = c(1.092, 1.328, "...", 1.425, 2.388, 2.392),
  Modelo_3 = c(1.084, 10.956, 11.181, "...", 2.381, 2.387),
  Modelo_4 = c(1.021, 1.021, "...", "...", 2.364, 2.387),
  Modelo_5 = c(1.040, "...", 1.042, "...", 2.376, 2.386),
  Modelo_6 = c(1.090, "...", "...", 1.095, 2.384, 2.378),
  Modelo_7 = c(1.012, "...", "...", "...", 2.338, 2.356)
)

kable(
  mods_vifs,
  caption = "Nuevos VIFs"
)

```
AICs de los modelos:

- Modelo 1: 1804.49
- Modelo 2: 1804.60
- Modelo 3: 1804.36
- Modelo 4: 1802.60
- Modelo 5: 1802.52
- Modelo 6: 1802.63
- Modelo 7: 1800.64

A excepción del modelo 3, ya ninguna presenta multicolinealidad. Pero el modelo 7 consiste del AIC mas bajo en comparación con los demás nos indica que el mejor modelo que mejor explica la quema de calorias es aquel excluyendo las variables: *Duration*, *Height*, *Weight* y *BMI*. Ajustando el modelo obtenemos los siguientes resultados.

```{r}

mod <- lm(Calories ~ Age + Heart_Rate + Body_Temp, calories)
mod_summary <- summary(mod)

coeffs <- data.frame(
  mod_summary$coefficients
)

kable(
  coeffs,
  caption = "Resumen del nuevo modelo",
  digits = 6
)

sd(residuals(mod))
```
- Multiple R-squared: 0.8902
- Adjusted R-squared: 0.8885
- P-valor conjunto: $<2.2\times 10^{-16}$

## 5.4. Análisis de residuos

En la sección anterior se mencionó que se utilizaba el modelo que excluía la variable *Duration* con todo y que tenia el mayor AIC de los tres modelos. La razón es por los supuestos. Era el único modelo que cumple con los supuestos de normalidad y autocorrelación en los residuos. Sin embargo, todavía tenemos heterocedasticidad.

**Normalidad.** Se evalúa si los residuales siguen una distribución aproximadamente normal. Mediante un QQ-plot, un histograma de frecuencias y un boxplot, para una interpretación visual. Se usan las pruebas de normalidad de Lilliefors, Cramér-von Mises, Anderson-Darling y Shapiro-Wilk.

```{r}
par(mfrow = c(1, 3))

qqnorm(residuals(mod))
qqline(residuals(mod))

hist(residuals(mod), breaks = 20)

boxplot(residuals(mod), main = "Boxplot")


norm_tests <- data.table(
  Test = c(
    "Lilliefors", 
    "Cramér-von Mises", 
    "Anderson-Darling", 
    "Shapiro-Wilk"
  ),
  p_value = c(
    lillie.test(mod$residuals)$p.value,
    cvm.test(mod$residuals)$p.value,
    ad.test(mod$residuals)$p.value,
    shapiro.test(mod$residuals)$p.value
  )
)

kable(
  norm_tests,
  caption = "Pruebas de normalidad",
  digits = 4
)
```

**Homocedasticidad**. Se muestra el gráfico de dispersión de residuales contra valores ajustados, así como las pruebas formales de homocedasticidad.

```{r}
df <- data.frame(
  fitted = fitted(mod),
  resid = residuals(mod)
)

ggplot(df, aes(x = fitted, y = resid)) +
  geom_point()

hc_test <- data.frame(
  Test = c("Breusch-Pagan", "NCV"),
  p_values = c(bptest(mod)$p.value, ncvTest(mod)$p)
)

kable(
  hc_test,
  caption = "Pruebas de homocedasticidad",
  digits = 4
)

```

**No correlación**. Finalmente, se evalúa la presencia de autocorrelación en los residuales mediante la prueba correspondiente.

```{r}
autocor_test <- data.frame(
  Test = "Durbin-Watson",
  p_value = dwtest(mod)$p.value
)

kable(
  autocor_test,
  caption = "Prueba de autocorrelación"
)

```
